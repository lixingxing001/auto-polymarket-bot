# 候选策略登记与受控对照实验

## 原则

- 每个候选都必须先登记，再观察未来窗口
- 登记时点之前的窗口一律不能计入候选实验
- 候选只和同一批未来窗口上的激活策略比较
- 候选赢了，也不能自动升级，仍需经过门控审查

## 当前首个候选

- `candidate_id = edge_008`
- 假设：更高的价格优势门槛可能比单纯提高置信度更稳
- 参数：
  - `min_confidence = 0.65`
  - `min_edge = 0.08`
  - `stake_usd = 10.0`
  - `max_fill_delay_seconds = 30`

## 为什么先试它

此前的成交代理复盘里，`0.65 / 0.08` 在 holdout 上比当前激活参数更像有交易意义，但那只是一条离线假设。现在把它登记为候选，让它只在未来窗口里接受检验。

## 第二个候选方向

- 目标：避开 `1 分钟动量弱` 且 `距离 barrier 近` 的窗口
- 来源：误差分层诊断里，这两个切片都只有 `40.0%` 准确率
- 规则：若 `abs(return_1m)` 与 `abs(distance_to_barrier_bps)` 同时低于门槛，则候选策略强制 `HOLD`

## 当前状态

- `edge_008` 已登记
- 登记后的可评估起点：`2026-05-18T15:05:00+00:00`
- 当前合格未来窗口：`0`
- 这个 `0` 是正确结果，因为系统还没有资格拿旧窗口给候选加分
