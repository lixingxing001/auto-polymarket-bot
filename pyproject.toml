[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "btc5m-bot"
version = "0.1.0"
description = "Research scaffold for a BTC 5-minute Polymarket trading bot"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
